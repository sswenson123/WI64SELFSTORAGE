User-agent: *
Allow: /

# Note: .html URLs are intentionally left crawlable so Google can read the
# rel=canonical on each one pointing to the extensionless version.
# Do NOT re-add "Disallow: /*.html$" — it blocked the site's own navigation.

# Block backend/portal pages from indexing (those are on storedge.com anyway)
Disallow: /wp-admin/
Disallow: /wp-login.php

# Sitemap location — submit this URL in Google Search Console
Sitemap: https://wi64selfstorage.com/sitemap.xml
